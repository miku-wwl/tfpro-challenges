# TODO 1: csvdecode 并规范化 direction/protocol/ports/CIDR/enabled。
# TODO 2: 建立独立的 identity、tuple、overlap、字段、端口、CIDR checks。
# TODO 3: 由规范化复合 key 创建 VPC、SG 和独立 ingress/egress rule resources。
# TODO 4: 用 data.aws_vpc 与 data.aws_security_group 回读真实网络。
